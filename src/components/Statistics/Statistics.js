export default function Statistics({good, neutral, bad}) {
    return (
       
        <section>
           
           <span>Good: { good} </span>
            <span>Neutral: { neutral} </span>
            <span>Bad: { bad} </span>
                    </section>
    )
}